<%= form_with model: @profile, local: true do |f|%>

  <%= f.fields_for :user, User.new do |user_form| %>
    <%= user_form.label :email%>
    <%= user_form.text_field :email%>
    
    <%= user_form.label :password%>
    <%= user_form.text_field :password%>

    <%= user_form.label :role %>
    <%= user_form.select(:role, options_for_select([["Admin", "admin"], ["Funcionário", "employee"]]))%>
  <% end %>

  <%= f.label :full_name%>
  <%= f.text_field :full_name%>

  <%= f.label :cpf%>
  <%= f.text_field :cpf%>

  <%= f.label :address%>
  <%= f.text_field :address%>

  <%= f.label :subsidiary_id%>
  <%= f.collection_select(:subsidiary_id, @subsidiaries, :id, :name)%>

  <%= f.submit 'Criar'%>
<%end%>

